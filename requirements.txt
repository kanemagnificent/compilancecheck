# Pack Proof — Legal Metrology Compliance Engine
# Core dependencies

flask>=3.0
werkzeug>=3.0
jinja2>=3.1

# OCR (optional — demo mode works without these)
# easyocr>=1.7
# opencv-python>=4.8
# numpy>=1.24
# pytesseract>=0.3

# PDF generation (optional — falls back to HTML)
# weasyprint>=60.0
